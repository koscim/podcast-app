<div class="container homepage">
  <h1>WELCOME TO POD,</h1>
  <h1>THE INTELLIGENT PODCAST PLAYER.</h1>
  <h4>BEFORE YOU CAN ACCESS YOUR PODCASTS, YOU MUST SIGN IN BELOW.</h4>
  <h4>IF YOU ARE NEW TO POD, PLEASE TAKE THE TIME TO SIGN UP.</h4>
  <div class="u-full-width">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <h4><%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
      <%- end -%></h4>
      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true %>
      </div>

      <div class="field">
        <%= f.label :password %><br />
        <%= f.password_field :password, autocomplete: "off" %>
      </div>

      <% if devise_mapping.rememberable? -%>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end -%>

      <div class="actions">
        <%= f.submit "Log in" %>
      </div>
      <%= render "devise/shared/links" %>
    <% end %>
  </div>
</div>
